// SPDX-License-Identifier: Apache-2.0

/**
 * @deprecated Import from `@webhook-portal/support-evidence/fail-closed-validation`
 * (or the package root) instead. This alias preserves the historical
 * `./sanitizer` subpath: the module performs fail-closed evidence *validation*,
 * not best-effort sanitization, and was renamed to say so.
 */
export * from "./fail-closed-validation.js";
