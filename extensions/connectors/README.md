# Connector configuration packs

Connector extensions are data-only configuration schemas and text templates.
They cannot execute provider calls.

- [AWS event destinations](aws-event-destinations/) — host-managed
  EventBridge/SQS-style destination configuration with least-privilege notes.
